services:

  db:
    image: mysql
    restart: always
    container_name: mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: autoplex 
      MYSQL_USER: myuser
      MYSQL_PASSWORD: mypass

